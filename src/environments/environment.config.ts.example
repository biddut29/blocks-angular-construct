/**
 * Committed template — copy is generated to `environment.config.ts` (gitignored)
 * on `npm start` / `env:apply`. Values are filled from environment variables.
 */
export const appConfig = {
  blocksApiUrl: '',
  apiBaseUrl: '',
  xBlocksKey: '',
  projectSlug: '',
  captchaSiteKey: '',
  captchaType: '',
} as const;
